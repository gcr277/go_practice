// int, float32 等不是保留关键字，但是属于预定义标识符，极不推荐作为变量名
// 包名，尽量和目录名保持一致。main函数单独放在main包
// 命名采用驼峰命名法
// a%b == a - a/b*b
// ++ 和 -- 只能当作独立的语句使用，不能++后赋值
//    && || 为短路与  短路或
// go没有三元运算符
// & | ^ 按位与或异或
// 右移 低位溢出，符号位不变，并用符号位填充高位（算术右移）
// 左移 高位溢出，符号位不变，低位填充0（算术左移）